#!/bin/bash
PROGA=proga
PIPE=/tmp/testpipe

DELAY=5


if test -x "$PROGA"; then

if [[ ! -p "$PIPE" ]]; then
mkfifo "$PIPE"
else

if [[ ! -p "$PIPE" ]]; then
echo "Reader not running"
exit 1
else

fi
fi
else
echo "исполняемый файл $PROGA не существует"
fi
